%table.table.table-striped
  %thead
    %tr
      %th
        Имя
      %th
        Работает?
      %th
        Когда работает?
      %th
        = '...'
  %tbody
    - @girls.each do |girl|
      %tr
        = simple_form_for girl, url: manager_girl_path(girl) do |f|
          %td
            = girl.name
          %td
            = f.input :available_today, as: :radio, label: false
          %td
            = f.input :available_date, label: false
          %td{valign: :middle}
            = f.submit 'Применить', class: [:btn, 'btn-success']